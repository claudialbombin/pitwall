# pitwall — Python dependencies
# Install with: pip install -r requirements.txt
#
# For the full dev environment (tests + notebooks):
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Minimum Python version: 3.10

# ── Core numerical stack ──────────────────────────────────
numpy>=1.26,<2.0
scipy>=1.12
pandas>=2.1
pyarrow>=14.0           # parquet I/O (data/processed/)

# ── Machine learning ──────────────────────────────────────
scikit-learn>=1.4       # GaussianProcessRegressor, kernels

# ── F1 telemetry ─────────────────────────────────────────
fastf1>=3.3             # FastF1 API — requires internet on first run

# ── Visualisation (notebooks only) ───────────────────────
matplotlib>=3.8